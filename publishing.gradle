def arena = [:]
arena.name = "arena"
arena.version = "1.0"
ext.arena = arena

def fragmentNavigation = [:]
fragmentNavigation.name = "fragment-navigation"
fragmentNavigation.version = "0.9"
ext.fragmentNavigation = fragmentNavigation

def mviCore = [:]
mviCore.name = "mvi-core"
mviCore.version = "1.0"
ext.mviCore = mviCore

def camp = [:]
camp.name = "camp"
camp.version = "1.0"
ext.camp = camp

def vkontakte = [:]
vkontakte.name = "vkontakte"
vkontakte.version = "0.9"
ext.vkontakte = vkontakte

def simpleFtp = [:]
simpleFtp.name = "simple-ftp"
simpleFtp.version = "0.9"
ext.simpleFtp = simpleFtp

def simpleTelnet = [:]
simpleTelnet.name = "simple-telnet"
simpleTelnet.version = "0.9"
ext.simpleTelnet = simpleTelnet